using Ihale.Entity.Concrete;

namespace Ihale.Business.Abstract
{
    public interface IUserService
    {
        //void Login(string userName,string password,bool rememberMe);
        //BaseResponse Register(RegisterModel model);
        //void LogOut();
        //bool ChangeProfilePic(string path);
        //User GetUserData();
        //BaseResponse SetUserData(User user);
        //BaseResponse SetUserPassword(string newPassword, string newConfirm, string oldPassword);
    }
}